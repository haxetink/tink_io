sudo: required
dist: trusty

language: node_js
node_js: 6

os:
  - linux
      
install:
  - npm install

script:
  - npm run travix node
  - npm run travix neko
  #- npm run travix java
  #- npm run travix interp
  - npm run travix php
